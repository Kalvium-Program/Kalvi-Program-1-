// Sample arrays
const numbers = [1, 2, 3, 4, 5, 6];
const words = ['apple', 'banana', 'cat', 'dog'];
const letters = ['a', 'b', 'c', 'd'];

// 1. Use  map() to square each element in the 'numbers' array and return it
function squareArray(arr) {
  
}

// 2. Using basic JavaScript variables and operators, calculate the product of all elements in the numbers array and return the result.
function calculateProduct(arr) {
  let product = 1;
  // Use a for loop and multiplication operator to calculate the product
}

// 3.Using string variables and the concatenation operator, create a single string that joins all words in the words array, separated by spaces, and return the resulting string.
function concatenateWords(arr) {
  let result = '';
  // Use a loop and the concatenation operator to join the words
}

// 4.Use the push() method to add the number 7 to the numbers array and return the updated array.
const numbers1 = [1, 2, 3, 4, 5, 6];
function addElement(arr) {
  // Use push to add the number 7 to the array
}


// 5.Use the pop() method to remove the last element from the numbers array and return the updated
const numbers2 = [1, 2, 3, 4, 5, 6];

function removeLastElement(arr) {
  // Use pop to remove the last element from the array
}
